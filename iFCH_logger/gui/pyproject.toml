[project]
name = "iFCH_logger_gui"
version = "0.1"
description = "GUI for iFCH Logger"
requires-python = ">=3.12"
dependencies = [
    "pyserial",
    "pyserial-asyncio",
    "qasync",
    "PySide6>=6.9.2",
    "numpy",
    "pip>=25.2",
]
